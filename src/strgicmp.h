#ifndef STRGICMP_H
#define STRGICMP_H

#include "autotrace.h"

extern bool strgicmp (const char *s1, const char *s2);
extern bool strgnicmp (const char *s1, const char *s2, size_t len);

#endif /* not STRGICMP_H */
